# Environment Variables for BookManagement API

# ASP.NET Core Environment (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Production

# ASP.NET Core URLs (default: http://+:8080)
ASPNETCORE_URLS=http://+:8080

# Database Connection String
# For SQLite: Data Source=/app/data/books.db
# For other databases, update accordingly
ConnectionStrings__DefaultConnection=Data Source=/app/data/books.db

# Logging Configuration (optional)
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft=Warning

# Allowed Hosts (optional, default: *)
AllowedHosts=*
